[package]
name = "reg-rs-cli-bench"
version = "0.1.0"
edition = "2024"

[dev-dependencies]
criterion = { version = "0.5", features = ["html_reports"] }
reg-rs-discover = { path = "../discover" }
tempfile = "3"

[[bench]]
name = "cli_benchmarks"
harness = false
