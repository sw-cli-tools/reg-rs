[package]
name = "reg-rs-core-bench"
version = "0.1.0"
edition = "2024"

[dev-dependencies]
criterion = { version = "0.5", features = ["html_reports"] }
reg-rs-types = { path = "../types" }
reg-rs-store = { path = "../store" }
reg-rs-store-rgt = { path = "../store-rgt" }
tempfile = "3"

[[bench]]
name = "core_benchmarks"
harness = false
