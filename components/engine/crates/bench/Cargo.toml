[package]
name = "reg-rs-engine-bench"
version = "0.1.0"
edition = "2024"

[dev-dependencies]
criterion = { version = "0.5", features = ["html_reports"] }
reg-rs-types = { path = "../../../core/crates/types" }
reg-rs-runner = { path = "../runner" }

[[bench]]
name = "engine_benchmarks"
harness = false
