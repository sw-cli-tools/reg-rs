* rtt1/doc/plan.org
** deps
*** chrono - timestamps
*** rusqlite - db
*** structopt - CLI arg parsing
*** walkdir - disover tests in subdirectories
** POCs
*** clap5
*** db6
*** structopt2
*** wdir2
** MVP
*** create, discover, dry-run, run, report, smart-diff?
** research
*** template library for report
** physical design
*** src: args, builder, config, db, error, lib, process, report, runner, status
** future
*** serve web console, status (running tests), results (reports), graphs?
*** smarter diff
*** regex
** completed
*** DONE refactor db6, structopt2 to use structopt in rtt1 instead of clap
*** DONE add debug flag from clap5 args in rtt1 instead 
*** DONE refactor clap5 to support dry-run, report in rtt1
*** DONE refactor wdir2 to support test discovery in rtt1
*** DONE add string_template crate to produce reports
*** DONE refactor to use tinytemplate instead of string_template crate 
*** DONE move now to new time module
*** DONE extract SQL strings to new queries module
*** DONE add more details to reports (nested contexts/templates)
*** DONE refactor template(s) to separate module
*** DONE implement dry_run
*** DONE run discovered tests
*** DONE check for regressions (stdout)
*** DONE check for regressions (exit code, stderr)
*** DONE split discover out of runner (into new finder module)
*** DONE store created test results using original single row table
*** DONE use templates to generate sql statements (dry)
*** DONE store latest results in database using single row table 
*** DONE new subcommand: remove - removes original and results tables in one or more databases
*** DONE store regressions (diff chunks with types) in database
*** DONE add differences template and context
*** DONE refactor reports into summary, detail, and differences reports
*** DONE report summary should show pass/fail (count of pasing tests, count of failing tests)
*** DONE report detail should show list of failing tests 
*** DONE report detail should show list of passing tests 
** next
*** TODO add more details (e.g. date created, date last ran, kind of failure, number of differences detected)
*** TODO report differences should show differences per failing test
*** TODO add verbosity levels to report (summary always, -v list failing, -vv list failing/passing -vvv show chunks)
*** TODO clean up, add unit-tests, doc tests, (e.g. remove hardcoded data dir, add .tdb when missing)
*** TODO dogfood test tests, rtt tests
*** TODO add examples
*** TODO add status server (status module) skeleton
*** TODO handle bad test databases and other errors (errors module)
*** TODO add error handling in main (try_main?)
*** TODO add exit codes (rtt1 should return non-zero if there is a test regression or rtt1 error)
*** TODO add panics for internal logic errors? (code coverage unreachable paths?)
*** TODO time test execution (store in db)
*** TODO watchdog timer thread to time-out test threads, main thread waits for done or time-out
*** TODO store timeouts and results in db (enum Fail, Pass, TimedOut)
*** TODO add timestamps, times, timeouts and results to generated reports
*** TODO add time-based examples
*** TODO add status server (status module) server sent events, wasm?
*** TODO add graphics (color in tree of tests running, passing, failing; interactive nodes?)
*** TODO add benchmarks
*** TODO run tests in parallel (timers in parallel, too) - parallel flag? default parallel?
*** TODO add parallel examples
*** TODO update benchmarks
*** TODO update documentation
deferred
*** TODO change sql statements from str to types? (so you can't pass a drop by accident)
